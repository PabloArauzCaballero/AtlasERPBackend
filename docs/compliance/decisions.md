# Decisiones del plan de cumplimiento

Cada sección registra una decisión tomada por defecto (conservadora y configurable), la
alternativa descartada y el rol que debe ratificarla.

## P-03 · Entrega real del outbox ERP (B05)

| #   | Decisión tomada                                                                                                                                                                                                                                                                                    | Alternativa                                                                                                                                                                                      | Ratifica                  |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------- |
| 1   | Transporte: HTTP POST firmado con HMAC-SHA256 (`x-atlas-signature: t=…,v1=…`) a `OUTBOX_DELIVERY_URL`; el ACK duradero es un 2xx que el receptor responde sólo tras confirmar su inbox.                                                                                                            | Kafka/SNS/RabbitMQ/Redis Streams: el equipo no opera ningún broker y el ERP no tiene cliente de Redis ni dependencia de colas; los servicios de Atlas ya se hablan por HTTP servicio-a-servicio. | Equipo de integración (I) |
| 2   | Sin `OUTBOX_DELIVERY_URL` el worker NO entrega ni marca: los eventos quedan PENDING, se avisa cada minuto y `/accounting/outbox/status` responde `transportConfigured: false`. Con URL, el secreto (≥ 32) es obligatorio.                                                                          | Seguir marcando publicado «en local» (lo que causaba B05).                                                                                                                                       | E                         |
| 3   | Clasificación de respuestas: 2xx = ACK; 400/410/413/415/422 = rechazo definitivo → DEAD inmediato; 401/403/404, 3xx, 408, 429, 5xx, red y timeout = transitorio con backoff. 401/403/404 se tratan como configuración (secreto rotado, receptor no desplegado) para no mandar toda la cola a DEAD. | Todo 4xx definitivo.                                                                                                                                                                             | I                         |
| 4   | Reintentos por defecto: 12 intentos, backoff `5 s · 2^(n−1)` con tope de 1 h; lease 60 s (≥ 2 × timeout de 10 s). Agotado → DEAD, visible, sin reenvío automático.                                                                                                                                 | Reintentos infinitos (ocultan fallos) o DLQ en otra tabla.                                                                                                                                       | E + operación             |
| 5   | Orden por agregado estricto: el worker no entrega la versión N+1 mientras la N esté PENDING o DEAD (bloqueo de cabeza de línea deliberado).                                                                                                                                                        | Entrega desordenada confiando sólo en la versión del consumidor.                                                                                                                                 | I                         |
| 6   | Filas antiguas con `published_at` (marcadas por el worker que sólo escribía un log): se etiquetan `LEGACY_LOG_ONLY`, se conserva su `published_at` y NO se reenvían solas; un replay autorizado puede hacerlo.                                                                                     | Reenviar todo el histórico al configurar el receptor (inundaría al consumidor con hechos viejos sin decisión humana).                                                                            | E + dueño del consumidor  |
| 7   | Replay: sólo roles `ADMIN`, `CFO`, `FINANCE`; exige motivo (10–500 caracteres) y queda en `business_action_logs`; sólo DEAD o LEGACY_LOG_ONLY (nunca un PUBLISHED).                                                                                                                                | Replay libre por script sin auditoría.                                                                                                                                                           | Finanzas (CFO)            |
| 8   | Inbox: `(consumer, event_key)` único. Dos modos: `every-event` (efectos que suman, p. ej. una obligación por evento) y `latest-state` (proyecciones: una versión vieja se descarta como STALE).                                                                                                    | Un único modo «última versión gana», que perdería efectos económicos que llegan tarde.                                                                                                           | I                         |

Pendiente de ratificar fuera de este paquete: SLO de edad máxima de un evento PENDING y la alerta
que lo vigile (UC_MonitorearEventos); qué servicio expone el receptor del outbox ERP y el despliegue
del proceso `worker:outbox` (hoy no figura en ningún compose ni en Coolify).
# Decisiones del plan de cumplimiento (ERP)

Cada sección la escribe el paquete que tomó la decisión. Una decisión de negocio, riesgo, fiscal o
legal se implementa con un valor por defecto conservador y queda aquí PENDIENTE DE RATIFICAR por el
rol indicado.

## P-06 · Puente de factura de comercio al mayor (B09) — 2026-09-24

| #   | Decisión                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Alternativa descartada                                                                     | Ratifica     |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ------------ |
| 1   | `PATCH /b2b/billing/invoices/:id/post-to-gl` genera un **BORRADOR** (DRAFT) y no publica. La publicación es el paso explícito y separado `PATCH /accounting/documents/:id/post` (bloqueo de fila, período abierto, doble partida sobre lo guardado, aprobación, hash). La respuesta expone `accountingDocumentStatus`, `journalPostingStatus`, `postedToLedger` (sólo `true` si está POSTED), `outcome` y `nextStep`. La ruta conserva su nombre por compatibilidad.                                                                                                                                  | Publicar dentro del puente: saltaría la revisión humana del asiento antes de contabilizar. | R (Finanzas) |
| 2   | Crear el documento y enlazarlo a la factura ocurre en UNA transacción que empieza con `SELECT … FOR UPDATE` de la factura. Una segunda llamada (simultánea o posterior) devuelve el mismo documento (`EXISTING_DOCUMENT_RETURNED`).                                                                                                                                                                                                                                                                                                                                                                   | Reintentar tras violar un índice único: deja el error al cliente.                          | E            |
| 3   | Unicidad por origen contable en base: `uq_accounting_document_merchant_invoice_origin` (CRM, MERCHANT_INVOICE, id de factura; sin libro, excluye VOID) y `uq_merchant_invoices_accounting_document`. Un documento huérfano de la versión anterior (que escribía en dos transacciones) se enlaza (`ORPHAN_DOCUMENT_LINKED`) en vez de crear otro.                                                                                                                                                                                                                                                      | Crear uno nuevo y anular el huérfano: duplica numeración y audita peor.                    | E            |
| 4   | Una factura REVERSADA sigue enlazada a su documento original; el puente la devuelve con estado `REVERSED` y no crea otro. Volver a contabilizarla exige una factura nueva (o nota de crédito).                                                                                                                                                                                                                                                                                                                                                                                                        | Permitir re-contabilizar el mismo origen tras el reverso: rompe la unicidad por origen.    | R            |
| 5   | Revalidación: partner = el del comercio de la factura (un `partnerId` distinto se rechaza, 409 `MERCHANT_INVOICE_PARTNER_MISMATCH`; sin partner vinculado se rechaza aunque el cliente envíe uno); moneda = la de los cargos de la factura (una sola) = la solicitada = la moneda base de la entidad legal (sin tipo de cambio registrado no se inventa importe local); subtotal + impuesto = total y cabecera = suma de líneas; contrato, si existe, del mismo comercio; factura en estado ISSUED/PARTIALLY_PAID/PAID/OVERDUE. Entidad fuera del alcance del usuario → 403 antes de leer la factura. | Confiar en lo que envía el cliente.                                                        | E + R        |
| 6   | `postDocument` rechaza documentos con `approval_status` PENDING o REJECTED (409 `ACCOUNTING_DOCUMENT_APPROVAL_REQUIRED`). Hoy no existe endpoint para aprobar un documento contable: un documento creado como PENDING no se puede publicar hasta que exista ese flujo (falla cerrado).                                                                                                                                                                                                                                                                                                                | Seguir publicando sin mirar la aprobación.                                                 | R            |
| 7   | `journal_entry.journal_no` deja de ser único en toda la instalación (era `<documento>-JRN` y el número de documento es único por entidad: la segunda empresa chocaba y su alta/reverso daba 500). La unicidad pasa a «un asiento por documento» (`uq_journal_entry_document`).                                                                                                                                                                                                                                                                                                                        | Cambiar el formato del número de asiento.                                                  | E            |

Notas:

- Los índices únicos se crean sólo si los datos desplegados ya los cumplen; si no, la migración emite
  `WARNING` y no los crea (para no tumbar la API al arrancar). Tras desplegar, comprobar en el log
  de migración que no hubo `P-06:` WARNING. No se probó contra una copia de la base desplegada
  (fuera del alcance de este agente): hacerlo antes de promover.
- Hallazgo sin corregir (P-13): `GET /accounting/documents/:id` no comprueba la entidad legal del
  documento contra el alcance del usuario.
- Hallazgo sin corregir: la línea de CxC del puente usa `referenceType = 'MERCHANT_INVOICE'`; si la
  cuenta de CxC es de control, el validador/trigger exige una referencia de submayor de su lista y
  el borrador se rechaza (`CONTROL_ACCOUNT_REQUIRES_SUBLEDGER_REFERENCE`). Decidir con Finanzas si
  `MERCHANT_INVOICE` entra en la lista de submayores.

## P-07 · Dinero, MDR, impuestos y redondeo (B10) — 2026-09-24

### Límite único de conversión

`src/common/money/decimal-amount.util.ts`: todo importe se convierte una vez a unidades menores
`bigint` (`parseMoney`, `parseRatePercent`, `toMinorUnits`), se opera en enteros y sale con
`fromMinorUnits`. Un `number` de un cuerpo JSON se admite sólo si es la representación binaria de un
valor con la escala de su moneda y está dentro del entero seguro; por encima (≈ 90 billones) el
importe debe viajar como texto. Más decimales de los permitidos, negativo, fuera de `numeric(18,2)`
o moneda desconocida: se RECHAZA, no se redondea.

| #   | Decisión                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Alternativa                                                            | Ratifica     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ------------ |
| 1   | Redondeo `HALF_UP` (medio céntimo alejándose de cero) para BOB y USD. Es el que ya aplicaban `toMinorUnits` y `Math.round` sobre positivos: ningún importe ya emitido cambia.                                                                                                                                                                                                                                                                                                               | `HALF_EVEN` (bancario), disponible en `divideRounded(…, 'HALF_EVEN')`. | R (Finanzas) |
| 2   | Escala de tasas: 6 decimales (`numeric(9,6)`), rango 0–100 %.                                                                                                                                                                                                                                                                                                                                                                                                                               | —                                                                      | R            |
| 3   | Monedas admitidas: BOB y USD, escala 2. Cualquier otra se rechaza hasta añadirla con su escala.                                                                                                                                                                                                                                                                                                                                                                                             | Escala 2 por defecto.                                                  | R            |
| 4   | MDR = compra × tasa de la versión contractual VIGENTE AL COMPRAR (`findActiveContractVersion` con la fecha de la compra), acotado por mín./máx. de la regla. La factura reutiliza el importe del cargo; nunca recalcula con el contrato vigente al facturar. La compra guarda además `mdr_rate_percent`, `mdr_amount`, `mdr_rule_id`, `mdr_pricing_source` (las reglas MDR se editan en sitio: sin la instantánea no se podía reconstruir la comisión). Compras anteriores quedan con NULL. | Sólo `contract_version_id`.                                            | R            |
| 5   | Residuo de cualquier reparto: truncado proporcional y el residuo ENTERO a la PRIMERA parte (`allocateProportionally`). 100,00/3 = 33,34 + 33,33 + 33,33; 1.000,00 con pesos 4/3/3 = 400,00 + 300,00 + 300,00.                                                                                                                                                                                                                                                                               | Residuo a la última parte.                                             | R            |
| 6   | El ERP NO genera cronogramas de cuotas: los recibe (Core) y ahora exige suma = financiado EXACTA (antes ±0,01) y financiado = compra − inicial EXACTO. El inicial (60 %) conserva ±0,01 porque quien lo calcula puede redondear distinto un medio céntimo. Core debe usar la regla #5 al generar cuotas (contrato a fijar en P-14).                                                                                                                                                         | —                                                                      | R + C        |
| 7   | IVA de factura de comercio: se calcula sobre el subtotal (como antes) y se REPARTE entre líneas con la regla #5, de modo que Σ IVA líneas = IVA cabecera. Antes cada línea redondeaba por su cuenta y podían diferir.                                                                                                                                                                                                                                                                       | Calcular por línea y sumar (cambiaría el IVA de cabecera ya emitido).  | J (Fiscal)   |
| 8   | Una factura no mezcla monedas (409 `MIXED_CURRENCIES`); un pago no se aplica a una CxC de otra moneda (409 `PAYMENT_CURRENCY_MISMATCH`).                                                                                                                                                                                                                                                                                                                                                    | —                                                                      | R            |
| 9   | Constantes tributarias: NO se tocaron. `DEFAULT_TAX_RATE_PERCENT` (env, defecto 13) y la regla del 60 % de inicial siguen como estaban; las valida Fiscal / Producto.                                                                                                                                                                                                                                                                                                                       | —                                                                      | J            |

### Inventario de conversiones a `Number` en caminos financieros (b2b-sales-crm y accounting)

Corregidas en P-07/P-06:

| Archivo                                                         | Qué hacía                                                                                                                                                                  | Ahora                                                                             |
| --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| `b2b-sales-crm/services/merchant-accounting-bridge.service.ts`  | `Number(invoice.totalAmount/subtotalAmount/taxAmount)` a las líneas del asiento                                                                                            | `parseMoney` → cadenas exactas                                                    |
| `b2b-sales-crm/services/b2b-bnpl-billing.service.ts`            | `toFixed` de entradas; subtotal/IVA con `reduce` + `roundMoney`; IVA por línea; asignación de pagos y saldo abierto en `number`; estado de factura por `=== 0` en flotante | `parsePurchaseSplit`, `computeInvoiceTotals`, `allocationsMatchPayment`, `bigint` |
| `b2b-sales-crm/services/b2b-sales-crm-use-case.base.ts`         | `calculateMdr` en flotante; `assertPurchaseAmounts` / `assertInstallmentsMatchFinancedAmount` con tolerancia 0,01                                                          | `computeMdrFee`, `assertPurchaseSplit` (dominio `merchant-billing-math.ts`)       |
| `b2b-sales-crm/b2b-sales-crm.schemas.ts`                        | `roundMoney` y tolerancia 0,01 en `registerPurchaseSchema` y `registerMerchantPaymentSchema`                                                                               | misma función de dominio que el servicio                                          |
| `b2b-sales-crm/services/b2b-reconciliation.service.ts`          | `listCommissions` sumaba con `Number`                                                                                                                                      | `sumMinor`                                                                        |
| `accounting/documents/services/accounting-documents.service.ts` | `Math.max(debit, credit)` para `amount_lc`; `Number(line.debit/credit/amountLc)` al publicar y al reversar                                                                 | cadenas exactas y comparación en `bigint`                                         |

Pendientes (fuera de los archivos de P-07 o de otro agente):

| Archivo:línea                                                   | Conversión                                                                                                                           | Dueño                              |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------- |
| `b2b-sales-crm/services/b2b-coverage.service.ts` (~140–149)     | `roundMoney(toNumber(amountRecovered) + input.amount)` en recuperaciones                                                             | agente de cobertura (P-04/P-05)    |
| `b2b-sales-crm/services/b2b-sales-crm-use-case.base.ts`         | `toNumber` / `roundMoney` se conservan sólo porque los usa cobertura; `calculateExpectedRevenue` (estimación comercial, no contable) | cobertura / P-15                   |
| `accounting/receipts/services/receipts.service.ts` (252–360)    | suma de asignaciones y saldo pendiente con `Number` y tolerancia 0,009                                                               | E (seguimiento P-07)               |
| `accounting/billing/services/billing.service.ts` (218, 352–487) | `gross = Number(net) + Number(tax)` y líneas del asiento AR en `number`                                                              | E (seguimiento P-07)               |
| `accounting/shared/schemas/accounting.schemas.ts`               | `money = z.coerce.number().multipleOf(0.01)` en el contrato HTTP de asientos                                                         | P-14 (contrato decimal como texto) |
| `accounting/supplier-payment-terms/*`                           | porcentajes y días con `Number` (no importes)                                                                                        | sin riesgo de céntimos             |
| `b2b-sales-crm/b2b-sales-crm.schemas.ts`                        | `money = z.coerce.number()` en el borde HTTP (se convierte una vez con `parseMoney` en el servicio)                                  | P-14                               |
# Decisiones de cumplimiento del ERP

Cada sección registra una política de negocio/riesgo que el plan de cumplimiento exigía y que se
implementó con un valor por defecto CONSERVADOR (fallar cerrado o mandar a revisión humana). Ninguna
está ratificada todavía: el rol indicado debe confirmarla o cambiarla.

## P-04 / P-05 · Cobertura BNPL, liquidación y recuperación (2026-09-24)

| #   | Decisión tomada                                                                                                                                                                                                                                                                                                                    | Alternativa considerada                                                                                                                                                                                                        | Ratifica                                                                                                |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- |
| 1   | **Día de negocio = día calendario en `America/La_Paz`** (`src/common/time/business-date.ts`). Una cuota está vencida cuando `due_date < día de negocio`: el día del vencimiento entero es plazo de pago. Lo usan el barrido y la elegibilidad.                                                                                     | Corte UTC (el anterior): daba por vencida la cuota 4 h antes del fin del día en Bolivia.                                                                                                                                       | R (negocio/riesgo)                                                                                      |
| 2   | **Plazo de verificación de un aviso REPORTED: 72 h** desde que el ERP lo recibe (`created_at`), configurable con `BNPL_PAYMENT_NOTICE_REVIEW_HOURS` (1–720). Dentro del plazo la cuota no se marca en mora ni se da por pagada; vencido, pasa a OVERDUE y el aviso entra en `coverage_review_items` (`PAYMENT_NOTICE_UNRESOLVED`). | Tratar REPORTED como pago (lo anterior, B08) o ignorarlo por completo (marcaría en mora a quien sí pagó).                                                                                                                      | R + Finanzas                                                                                            |
| 3   | **Cobertura sobre una cuota con aviso REPORTED → revisión**, no CxP. Aviso REJECTED no cuenta como pago.                                                                                                                                                                                                                           | Aprobar la cobertura por el total asumiendo que el aviso es falso.                                                                                                                                                             | R                                                                                                       |
| 4   | **Contrato no ACTIVE (suspendido, terminado, vencido) → revisión**, no CxP ni rechazo automático.                                                                                                                                                                                                                                  | Honrar siempre la cobertura por la versión contractual de la compra.                                                                                                                                                           | R + J (legal: vigencia de la obligación tras terminar el contrato)                                      |
| 5   | **Importe cubrible = saldo** (importe de la cuota − avisos CONFIRMED), en centavos exactos.                                                                                                                                                                                                                                        | Importe original (lo anterior, B06).                                                                                                                                                                                           | R + Finanzas                                                                                            |
| 6   | **Liquidación manual con doble control**: quien registra (`PATCH …/paid`) no puede confirmar ni rechazar (`…/settlement/approve                                                                                                                                                                                                    | reject`); lo impone el servicio (403) y un CHECK en la base. Ambos pasos exigen rol FINANCE o ADMIN. Evidencia = archivo ACTIVE en `atlas_accounting.erp_file`. Moneda de la CxP: BOB (no hay moneda por compra en el modelo). | Una sola firma con evidencia; o notificación bancaria autenticada (no existe proveedor: no se inventa). | Finanzas (política de doble control) |
| 7   | **Cambio de contrato HTTP**: `PATCH /b2b/coverage/payables/:id/paid` ya no acepta sólo `paidAt` (400) y responde 202 pendiente; `apply-payment` exige `paymentReference`. Es corrección de seguridad, no se mantiene la variante insegura. El frontend del ERP debe enviar los campos nuevos.                                      | Mantener `paidAt` como atajo (deja la brecha B07 abierta).                                                                                                                                                                     | E + dueño del frontend ERP                                                                              |
| 8   | **Reverso**: una CxP no liquidada se CANCELA (fila conservada, índice único parcial permite reabrir). Una CxP ya liquidada NO se cancela: su reverso (devolución del comercio) queda fuera de este paquete. Un cobro de recuperación se revierte con un movimiento compensatorio; los movimientos son de sólo inserción (trigger). | Borrar o editar filas (destruye historia).                                                                                                                                                                                     | Finanzas                                                                                                |
| 9   | **Sobrepago de recuperación → rechazo (409)** sin mutar; no se registra el excedente como saldo a favor.                                                                                                                                                                                                                           | Aceptar y dejar crédito a favor del consumidor (requiere política de devolución).                                                                                                                                              | Finanzas + J                                                                                            |

Pendiente fuera del ERP: la fuente de verdad de cuotas y avisos de pago (Core vs ERP, P-02/P-08)
decide quién escribe `consumer_payments_to_merchant` con estado CONFIRMED/REJECTED; hoy el ERP sólo
escribe REPORTED al registrar la compra.
