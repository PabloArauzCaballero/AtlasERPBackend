# Decisiones del plan de cumplimiento (AtlasERPBackend)

Cada paquete añade su propia sección. Una decisión aquí es un valor por defecto CONSERVADOR tomado
para poder avanzar; el rol indicado debe ratificarla o cambiarla.

## P-02 · Matriz de cumplimiento basada en evidencia

**Formato de requisitos.** `docs/compliance/requirements/<PAQUETE>.json`: array de
`{requirementId, packageId, repository, title, releaseIncluded, ownerRole, acceptance, tests[], commands?[], requiresDatabase, blockedBy, exclusion?}`.
`tests[].name` es el nombre completo de Jest (títulos de `describe` + `it` separados por espacios) o
un prefijo que identifique UNA sola prueba (si coincide con dos, cuenta como ambiguo y no verifica).
`commands[].name` remite a un sello de comando (`yarn compliance:stamp command <nombre> …`). La
clave única es `requirementId@packageId`; un mismo ID en varios paquetes se agrega en
`byRequirementId` y sólo está VERIFIED si lo está en todos.

**Estados.** DECLARED (sin prueba o con archivos inexistentes), IMPLEMENTED (la prueba existe pero
no corrió, falló, se saltó o es de otro SHA), VERIFIED (todas pasaron en el SHA candidato), BLOCKED
(`blockedBy`), DEFERRED (excluido con motivo, impacto y aprobador).

| Decisión                                                                                                                                                                                                                                                                              | Alternativa descartada                                                                                | Ratifica |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | -------- |
| Los 54 casos «Integración excluida» NO heredan la exclusión: quedan `releaseIncluded: null` con `pendingDecision`.                                                                                                                                                                    | Mantenerlos excluidos (el plan lo prohíbe: el encargo incluye la integración de los tres backends).   | R        |
| Los 46 «declarados operativos» se INCLUYEN en el release y deben demostrarse; hoy casi ninguno tiene prueba mapeada, así que cuentan como no verificados.                                                                                                                             | Dejarlos fuera del denominador hasta probarlos (inflaría el porcentaje).                              | R + L    |
| Los 107 MODEL_SUPPORTED y 49 extensiones sin prueba quedan `null` (decisión pendiente), fuera del denominador pero visibles, y hacen fallar el modo estricto.                                                                                                                         | Incluirlos todos (258/258) o excluirlos todos: ambas son decisiones de negocio que no toma un agente. | R        |
| UC_AsientoManual, UC_PublicarAsiento y UC_ReversarAsiento se incluyen porque tienen pruebas mapeadas (E2E + invariantes); los dos últimos eran MODEL_SUPPORTED.                                                                                                                       | —                                                                                                     | E        |
| Ningún caso se marca `false`: no hay aprobador. El campo `exclusion` existe; sin `approvedBy` la exclusión no vale y el caso sigue contando como incluido.                                                                                                                            | —                                                                                                     | R        |
| El modo estricto (`--strict`) corre en CI como INFORMATIVO (`continue-on-error`). Para volverlo bloqueante en release: quitar `continue-on-error` del paso «Modo estricto» del job `compliance` y añadir `compliance` a los `needs` del despliegue/promoción y a los required checks. | Bloquear ya: dejaría DEV sin desplegar hasta cerrar todo el plan.                                     | L + Q    |
| La evidencia de Jest se sella con el SHA y el sha256 del JSON (`scripts/compliance/stamp.ts`); sin sello, con otro SHA o alterada, se rechaza entera.                                                                                                                                 | Confiar en la fecha o en el nombre del artefacto.                                                     | Q        |
| `docs/audit/use-case-compliance-audit.{md,json}` pasan a generarse desde el evaluador (`yarn audit:use-cases`). La versión versionada es una foto sin resultados sellados (nada VERIFIED); la vigente es el artefacto `evidence-index` de cada run de CI.                             | Conservar el informe histórico con su «APROBADO».                                                     | L        |

## P-12 · CI del ERP con base migrada y controles financieros reales

| Decisión                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Alternativa descartada                                                                                                | Ratifica |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | -------- |
| El job `db-integration` migra con `yarn db:migrate:prod` (el mismo `dist/scripts/db/run-sql.js` que usa el servicio `migrate` del despliegue), dos veces para comprobar idempotencia.                                                                                                                                                                                                                                                                                            | Migrar con el arranque de la app (`STARTUP_MIGRATIONS_ENABLED`) o con `db:migrate`: no es lo que corre en despliegue. | S        |
| Ruta de actualización: desde `origin/test` (última versión promovida) y desde `origin/main` (primer commit, 9 archivos). Ambas son viables sin datos: se migra con los migradores de la versión previa y luego HEAD, y se exige esquema idéntico al de una instalación nueva (columnas, restricciones, índices, triggers y funciones; no el orden físico de columnas). No prueba migración de DATOS: requeriría un volcado representativo anonimizado, que no existe en el repo. | Sólo desde cero.                                                                                                      | S + E    |
| Los E2E con base usan una base propia (`atlas_erp_e2e`), porque el backend numera documentos por serie y la colisión de ERP-D01 haría depender el resultado del orden de las suites.                                                                                                                                                                                                                                                                                             | Compartir la base de integración.                                                                                     | Q        |
| `deploy-dev` depende además de `db-integration`. `dependency-audit`, `codeql`, `image-scan` y `compliance` NO bloquean todavía el despliegue a DEV: un aviso nuevo de terceros no debe dejar DEV sin desplegar; sí deben ser required checks del PR.                                                                                                                                                                                                                             | Hacerlos gate de despliegue ya.                                                                                       | S        |
| Auditoría de dependencias con `yarn audit` (nunca npm, que reescribe `yarn.lock`). Bloquea high/critical en dependencias de PRODUCCIÓN (las de la imagen, `--production`). Las de desarrollo se informan como aviso y artefacto; `AUDIT_FAIL_ON_DEV=true` las vuelve bloqueantes. El 2026-09-24: producción 5 moderate / 0 high; árbol completo 48 high (browserslist, fast-uri, js-yaml, sólo desarrollo).                                                                      | Bloquear también desarrollo (rojo permanente hoy) o no auditar.                                                       | S        |
| Trivy sobre la imagen de producción con el mismo criterio que el Motor: HIGH/CRITICAL con arreglo disponible bloquean el job; informe completo como artefacto.                                                                                                                                                                                                                                                                                                                   | —                                                                                                                     | S        |
| Las pruebas `*.integration.spec.ts` salen de la suite unitaria y van por `yarn test:integration`. Sin `DATABASE_URL` se saltan de forma visible en local; con `CI=true` o `REQUIRE_INTEGRATION_DB=true` la ausencia de base es un fallo.                                                                                                                                                                                                                                         | Dejarlas en `yarn test` saltándose en silencio.                                                                       | Q        |
| Defecto encontrado y NO corregido (ERP-D01): `journal_entry.journal_no` es único global pero el servicio lo deriva de `DOC-AAAA-NNNNNN`, serie por entidad legal; la segunda entidad que crea su primer asiento sin número recibe 409. Queda reproducido con `it.failing` (se pondrá rojo cuando se corrija).                                                                                                                                                                    | Cambiar el formato o la restricción: es una decisión de numeración contable.                                          | E + R    |
| Defecto corregido (ERP-D03): `createDraft` comprobaba el alcance por entidad legal DESPUÉS de numerar y deducir período/libro de la entidad pedida; un token de otra entidad recibía 400 con información de esa entidad. Ahora 403 antes de leer nada.                                                                                                                                                                                                                           | —                                                                                                                     | E        |
| Defecto corregido (ERP-D02): `yarn db:migrate` no migraba una base vacía (corría CRM antes que contabilidad y `20260915230000-dominios-alineados.sql` altera `atlas_accounting`). Ahora contabilidad va primero; CI compara su esquema con el de `db:migrate:prod`.                                                                                                                                                                                                              | —                                                                                                                     | S        |
