# Common

Componentes compartidos por toda la API: guards, pipes, filters, interceptors, decorators, tipos y persistencia reutilizable.

No debe contener reglas específicas del módulo CRM/Ventas B2B.

## Logging

- `logging/`: logger Pino centralizado, redacción de campos sensibles y adapter NestJS para logs por capa.
